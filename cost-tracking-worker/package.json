{
  "name": "cost-tracking-worker",
  "version": "1.0.0",
  "description": "Unit economics tracking worker: aggregates AI token spend and CI compute cost per ticket",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "report": "node src/nightly-report.js",
    "test": "jest --testPathPattern=tests/"
  },
  "dependencies": {
    "fastify": "^4.26.0",
    "node-cron": "^3.0.3",
    "pg": "^8.11.3",
    "pino": "^8.19.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
