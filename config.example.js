// Copy this file to config.js and fill in your configuration.
// config.js is gitignored to keep secrets out of source control.
//
// Option A — Backend proxy (recommended for production)
//   Deploy places-api-service and set PLACES_API_URL to its base URL.
//   The Foursquare API key lives on the server; the browser never sees it.
//
// window.PLACES_API_URL = 'https://your-places-api-service.example.com';
//
// Option B — Direct Foursquare calls (local development only)
//   Get a free API key at https://foursquare.com/developers/
//   WARNING: the key is visible in the browser; do not use in production.
//
// window.FOURSQUARE_API_KEY = 'YOUR_FOURSQUARE_API_KEY_HERE';
